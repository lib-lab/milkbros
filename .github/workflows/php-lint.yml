name: PHP Lint

on: push

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: php -l по всем файлам темы
        run: |
          set -e
          find . -name '*.php' -print0 | while IFS= read -r -d '' f; do
            php -l "$f"
          done
